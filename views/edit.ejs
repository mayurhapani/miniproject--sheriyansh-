<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="bg-zinc-900 h-screen w-full p-10">
      <div class="flex justify-end">
        <a class="block px-4 py-2 rounded-lg bg-red-500 text-white" href="/logout"> Logout</a>
      </div>
      <h2 class="text-white text-4xl mb-10 font-bold">Hello, You Can Edit Post From Here!</h2>

      <form action="/editPost/<%= post._id %>" method="post">
        <textarea
          class="resize-none bg-transparent p-3 w-1/3 outline-none border-2 border-zinc-600 rounded-lg text-white"
          name="content"
          placeholder="whats in your mind!"
        >
 <%= post.content %> </textarea
        >
        <input
          class="bg-yellow-500 p-3 rounded-lg w-40 mt-3 text-white block"
          type="submit"
          value="Edit Post"
        />
      </form>
    </div>
  </body>
</html>
