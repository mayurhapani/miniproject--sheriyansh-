<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="bg-zinc-900 h-screen w-full p-5">
      <div class="flex justify-end mt-5">
        <a class="block px-4 py-2 rounded-lg bg-blue-500 text-white" href="/login"> Login</a>
      </div>
      <h2 class="text-center text-white text-4xl mb-10 font-bold">Add User</h2>
      <form class="text-zinc-400" action="/addUser" method="post">
        <input
          class="bg-transparent border border-zinc-600 p-3 rounded-lg mb-3"
          type="text"
          name="name"
          placeholder="Enter name"
        />
        <input
          class="bg-transparent border border-zinc-600 p-3 rounded-lg mb-3"
          type="text"
          name="username"
          placeholder="Enter username"
        />
        <input
          class="bg-transparent border border-zinc-600 p-3 rounded-lg mb-3"
          type="email"
          name="email"
          placeholder="Enter email"
        />
        <input
          class="bg-transparent border border-zinc-600 p-3 rounded-lg mb-3"
          type="password"
          name="password"
          placeholder="Enter password"
        />
        <input
          class="bg-transparent border border-zinc-600 p-3 rounded-lg mb-3"
          type="number"
          name="age"
          placeholder="Enter age"
        />

        <input class="bg-blue-500 p-3 rounded-lg mb-3 text-white" type="submit" value="Submit" />
      </form>
    </div>
  </body>
</html>
